plugins {
    id "io.micronaut.build.internal.validation-module"
}

dependencies {
    annotationProcessor mn.micronaut.inject.java

    api projects.micronautValidation
    compileOnly libs.managed.jakarta.el
    runtimeOnly libs.jakarta.el.impl

    testAnnotationProcessor mn.micronaut.inject.java
    testImplementation mn.micronaut.inject.java.test
    testImplementation libs.managed.jakarta.el
    testImplementation mnTest.micronaut.test.junit5
    testRuntimeOnly libs.jakarta.el.impl
    testRuntimeOnly mnTest.junit.jupiter.engine
}

micronautBuild {
    binaryCompatibility {
        enabledAfter("5.1.0")
    }
}
